Firefox Android & Edge Desktop:
javascript:(async()=>{const SEL="div.x9FBF a[href*='/match/']";const A=[...document.querySelectorAll(SEL)];if(!A.length){alert("Inga matchlänkar hittades.");return}const enc=s=>s.replace(/&/g,"&amp;").replace(/</g,"&lt;");const fetchRefs=async(u,ms=1400)=>{try{const c=new AbortController;const t=setTimeout(()=>c.abort(),ms);const r=await fetch(u,{credentials:"include",signal:c.signal});clearTimeout(t);if(!r.ok)return[];const html=await r.text();const doc=new DOMParser().parseFromString(html,"text/html");let n=[...doc.querySelectorAll('a[href^="/domare/"]')].map(a=>a.textContent.trim()).filter(Boolean);if(!n.length)n=[...doc.querySelectorAll("td.wMqhM a")].map(a=>a.textContent.trim()).filter(Boolean);return n.slice(0,2);}catch(e){return[]}};const iframeRefs=u=>new Promise(res=>{const ifr=document.createElement("iframe");ifr.src=u;ifr.style.display="none";document.body.appendChild(ifr);let tries=0;const max=6;const tick=()=>{tries++;try{const d=ifr.contentDocument;if(d){let n=[...d.querySelectorAll('a[href^="/domare/"]')].map(a=>a.textContent.trim()).filter(Boolean);if(!n.length)n=[...d.querySelectorAll("td.wMqhM a")].map(a=>a.textContent.trim()).filter(Boolean);if(n.length||tries>=max){ifr.remove();res(n.slice(0,2));return}}}catch(e){}if(tries<max)setTimeout(tick,200);else{try{ifr.remove()}catch(e){}res([])}};ifr.onload=tick;setTimeout(()=>{try{ifr.remove()}catch(e){}res([])},2500)});const getRefs=async u=>{const n=await fetchRefs(u,1400);if(n.length)return n;return await iframeRefs(u)};let injected=0;for(const a of A){const card=a.closest("div.x9FBF");if(!card||card.dataset.refstatInjected)continue;card.dataset.refstatInjected="1";if(getComputedStyle(card).position==="static")card.style.position="relative";const panel=document.createElement("div");panel.className="refstat-panel";panel.style.cssText="position:absolute;left:50%;transform:translateX(-50%);bottom:8px;max-width:80%;background:rgba(255,255,255,.97);padding:6px 10px;font-weight:700;color:#002B5C;text-align:center;z-index:2000;pointer-events:none;font-family:inherit;font-size:inherit;line-height:inherit;";panel.textContent="Hämtar domare…";card.appendChild(panel);const m=new URL(a.getAttribute("href"),location.origin).toString();const lineup=m.endsWith("/laguppstallning")?m:m+"/laguppstallning";const refs=await getRefs(lineup);panel.innerHTML=refs.length?refs.map(x=>enc(x)).join("<br>"):"Domarinfo saknas";injected++;}console.log("✅ Paneler injicerade:",injected);if(!injected)alert("Ingen panel injicerades – ladda om sidan och kör igen (eller acceptera cookies).")})();